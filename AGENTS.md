# AGENTS.md

Public reusable core. Keep personal or project-specific material downstream.
Keep secrets, private endpoints, personal paths, and private Git history out.

Public overlay contract: later layers win; mappings merge recursively; lists
and scalars replace earlier values. Preserve compatibility or document a
versioned migration.
